name: Automate PR
on:
  issues:
    types:
      - labeled

permissions:
  contents: read

jobs:
  Automate:
    if: github.event.label.name == 'Automate'
    runs-on: ubuntu-latest
    permissions:
      issues: write
      actions: write
    
    steps:
      - uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b
        with:
          repository: step-security/secure-workflows
    
      - name: Automate PR
        uses: step-security/secure-workflows/Automate-PR@1d7d8da27f24b5200ea43357e928bedddc3137bc
        with:
          github-token: ${{secrets.PAT }}
          issue-id: ${{ github.event.issue.number}}