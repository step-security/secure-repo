name: Delete Package Versions
github-token:
  action-input:
    input: token
    is-default: true
  permissions:
    undefined: write
    undefined-reason: to <reason_here> # specify why undefined is used.