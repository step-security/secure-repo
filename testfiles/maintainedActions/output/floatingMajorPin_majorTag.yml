name: Paths Filter
on: pull_request

jobs:
  changes:
    runs-on: ubuntu-latest
    steps:
      - uses: step-security/paths-filter@v3
        with:
          filters: |
            src:
              - 'src/**'
