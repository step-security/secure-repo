name: "close issue"

on:
  push:
  

jobs:
  closeissue:
    runs-on: ubuntu-latest

    steps:
    - name: Close Issue
      uses: peter-evans/close-issue@v1
      with:
       issue-number: 1
       comment: Auto-closing issue
       
    - name: test case
      uses: evans/shield/@v1
      with:
       issue-number: 1
       comment: Auto-closing issue